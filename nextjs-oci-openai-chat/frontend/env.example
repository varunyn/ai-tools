# FastAPI backend URL
FASTAPI_BACKEND_URL=http://localhost:3001

# Optional: API key sent to backend (OpenAI-compatible auth). Default "local" for dev.
# FASTAPI_OPENAI_API_KEY=local

# Optional: Default model ID when the client does not send one (must exist on backend).
# MODEL_ID=meta.llama-4-scout-17b-16e-instruct

# MCP tools (SERVER-ONLY: do NOT expose to client bundles)
# Define all MCP servers as a JSON array. Each server is queried for tools; every tool is exposed to the chat.
# Optional per-server: authToken, timeoutMs (ms), prefix (prefix for tool names to avoid clashes).
#
# Example (single line or pretty-printed):
# MCP_SERVERS_CONFIG=[{"url":"http://localhost:3021/mcp","authToken":"","timeoutMs":15000,"prefix":"rag"},{"url":"http://localhost:3020/mcp","timeoutMs":10000,"prefix":"calc"}]
#
# Legacy fallback if MCP_SERVERS_CONFIG is unset: use MCP_ORACLE_RAG_URL and MCP_CALCULATOR_URL (no prefix).
MCP_SERVERS_CONFIG=

# Legacy: single URL per server (ignored when MCP_SERVERS_CONFIG is set)
MCP_ORACLE_RAG_URL=
MCP_CALCULATOR_URL=
MCP_ORACLE_RAG_AUTH_TOKEN=
MCP_CALCULATOR_AUTH_TOKEN=
